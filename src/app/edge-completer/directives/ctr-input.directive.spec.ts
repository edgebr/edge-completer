import { CtrlInputDirective } from './ctr-input.directive';

describe('CtrlInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlInputDirective();
    expect(directive).toBeTruthy();
  });
});
