import { CtrListDirective } from './ctr-list.directive';

describe('CtrListDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrListDirective();
    expect(directive).toBeTruthy();
  });
});
