import { CtrCompleterDirective } from './ctr-completer.directive';

describe('CtrCompleterDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrCompleterDirective();
    expect(directive).toBeTruthy();
  });
});
