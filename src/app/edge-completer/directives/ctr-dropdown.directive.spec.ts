import { CtrlDropdownDirective } from './ctr-dropdown.directive';

describe('CtrlDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
